[plugin]
name = "secret"
scope = "proto"
version = "0.1.0"
description = "Scan and mask leaked secrets in shell history"
author = "proto-cli"
repository = "https://github.com/proto-cli/plugins/tree/main/secret"

[plugin.commands]
secret = "bin/secret"
