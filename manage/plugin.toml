[plugin]
name = "manage"
scope = "proto"
version = "0.1.0"
description = "CLI self-management — update, uninstall, reset"
author = "proto-cli"
repository = "https://github.com/proto-cli/plugins/tree/main/manage"

[plugin.commands]
manage = "bin/manage"
