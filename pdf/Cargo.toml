[package]
name = "pdf"
version = "0.1.0"
edition = "2021"
description = "PDF toolkit — merge, split, compress, extract text"
license = "MIT"

[[bin]]
name = "pdf"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
owo-colors = "4"
