[plugin]
name = "status"
scope = "proto"
version = "0.1.0"
description = "Network monitoring — ping, monitor, serve dashboard, report"
author = "proto-cli"
repository = "https://github.com/proto-cli/plugins/tree/main/status"

[plugin.commands]
status = "bin/status"
