[plugin]
name = "dns"
scope = "proto"
version = "0.1.0"
description = "DNS lookup for domains"
author = "proto-cli"
repository = "https://github.com/proto-cli/plugins/tree/main/dns"

[plugin.commands]
dns = "bin/dns"
