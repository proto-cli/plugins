[plugin]
name = "docker"
scope = "proto"
version = "0.1.0"
description = "Interactive Docker container manager and safe prune"
author = "proto-cli"
repository = "https://github.com/proto-cli/plugins/tree/main/docker"

[plugin.commands]
docker = "bin/docker"
