[package]
name = "fmt"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "fmt"
path = "src/main.rs"

[dependencies]
proto-plugin-sdk = { path = "../shared" }
clap = { version = "4", features = ["derive"] }
