[package]
name = "cron"
version = "0.1.0"
edition = "2021"
description = "Human-readable crontab — next runs, validation, editor"
license = "MIT"

[[bin]]
name = "cron"
path = "src/main.rs"

[dependencies]
owo-colors = "4"
chrono = "0.4"