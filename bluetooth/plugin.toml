[plugin]
name = "bluetooth"
scope = "proto"
version = "0.1.0"
description = "Bluetooth manager TUI — scan, pair, connect, devices"
author = "proto-cli"
repository = "https://github.com/proto-cli/plugins/tree/main/bluetooth"

[plugin.commands]
bluetooth = "bin/bluetooth"
