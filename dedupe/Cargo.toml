[package]
name = "dedupe"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "dedupe"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
proto-plugin-sdk = { path = "../shared" }
sha2 = "0.10"
dialoguer = "0.11"
