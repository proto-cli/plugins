[package]
name = "todo"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "todo"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
proto-plugin-sdk = { path = "../shared" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "5"
