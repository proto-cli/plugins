[package]
name = "focus"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "focus"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
proto-plugin-sdk = { path = "../shared" }
