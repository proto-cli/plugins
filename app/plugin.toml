[plugin]
name = "app"
scope = "proto"
version = "0.1.0"
description = "Project diagnostics — doctor, port, nuke, snap"
author = "proto-cli"
repository = "https://github.com/proto-cli/plugins/tree/main/app"

[plugin.commands]
app = "bin/app"
